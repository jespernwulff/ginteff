% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ginteffplot.R
\name{ginteffplot}
\alias{ginteffplot}
\title{Plot a ginteff result}
\usage{
ginteffplot(
  x,
  output = 1L,
  obseff = FALSE,
  against = NULL,
  data = NULL,
  zeroline = TRUE,
  xlab = NULL,
  point_color = "black",
  range_color = "black",
  obs_color = "grey40",
  ...
)
}
\arguments{
\item{x}{A \code{ginteff} object.}

\item{output}{Integer index of the row of \code{aie} to plot when there
is more than one result (e.g. multi-level factor).
Default 1.}

\item{obseff}{Logical. If TRUE, the per-observation interaction
effects are plotted as a tick strip below the AIE.
Requires that \code{ginteff()} was called with \code{obseff = TRUE}.}

\item{against}{Optional name of a covariate. If supplied alongside
\code{obseff = TRUE}, the per-obs effects are plotted
against that covariate (a scatter on the y-axis is
produced instead of the strip layout). This is an R
extension and is not part of Stata's \code{ginteffplot}.}

\item{data}{Data frame to draw \code{against} from. Required when
\code{against} is set.}

\item{zeroline}{Logical. Draw a vertical reference line at x = 0.
Default TRUE.}

\item{xlab}{Optional x-axis label override.}

\item{point_color, range_color, obs_color}{Colours.}

\item{...}{Ignored (reserved for future options).}
}
\value{
A ggplot object (if ggplot2 is available) or NULL (base graphics).
}
\description{
Draws the average interaction effect (AIE) on a horizontal axis with
its capped-spike confidence interval, optionally overlaid with the
per-observation interaction effects as a thin strip below.
}
